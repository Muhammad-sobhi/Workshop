# مقارنة بين النموذج المحاسبي المعياري والوضع الحالي في النظام
# Comparison: Standard Inventory Accounting vs. Current ERP System Implementation

---

## 1. الملخص التنفيذي | Executive Summary

تتناول هذه المقارنة التحليلية الفرق بين **النموذج المحاسبي المعياري للمخزون والتصنيع (Inventory & COGS Standard Accounting)** والوضع الفعلي المطبق حالياً في كود النظام (`erp-backend`).

### النتيجة الرئيسية (Main Finding)
يقوم النظام الحالي بـ **تسجيل مشتريات المواد الخام كمصروفات فورية (Expenses)** في جدول `expenses` عند دفع العربون أو استلام الفاتورة، بدلاً من تسجيلها **كأصل (Inventory Asset)**. وفي الوقت نفسه، لا يتم احتساب **تكلفة البضاعة المباعة (COGS)** بشكل مباشر ودقيق عند البيع، بل يتم الاعتماد على معادلات استنتاجية تقريبية. 

هذا يؤدي إلى:
1. **مخالفة مبدأ المقابلة المحاسبي (Matching Principle)**: الاعتراف بالمصروف عند الشراء بدلاً من الاعتراف به عند تحقق الإيراد (البيع).
2. **مخاطرة ازدواجية الاحتساب (Double Counting Risk)**: إذا تم تسجيل شراء المواد كمصروف، ثم تم حساب تكلفة المنتج عند البيع كمصروف آخر.
3. **تشويه قائمة الأرباح والخسائر (Distorted P&L)**: تظهر الأرباح منخفضة جداً في أشهر الشراء والتصنيع، وتظهر مرتفعة جداً في أشهر البيع بدون خصم تكلفة البضاعة المباعة الفعلية.

---

## 2. جدول المقارنة السريعة | Quick Comparison Matrix

| المرحلة / الجانب (Stage / Aspect) | النموذج المحاسبي المعياري (Recommended Standard) | الوضع الحالي في النظام (Current Codebase) | تقييم الفجوة (Gap / Assessment) |
| :--- | :--- | :--- | :--- |
| **1. شراء المواد الخام (Raw Material Purchase)** | **أصل في الميزانية (Asset)**<br>`Inventory (Raw Material) ↑`<br>`Cash / Accounts Payable ↓`<br>*لا يوجد مصروف في P&L* | **مصروف فوري (Expense)**<br>يتم إنشاء سجل في جدول `expenses` تحت تصنيف `"شراء مواد خام"` عند دفع الدفعة المقدمة/الفاتورة. | ❌ **خلل جوهري**: الشراء ليس مصروفاً، بل تحويل نقدية إلى أصل مخزني. |
| **2. التصنيع والإنتاج (Manufacturing / Production)** | **تحويل بين الأصول (Asset Transfer)**<br>`Raw Material Inventory ↓`<br>`Work in Progress / Finished Goods ↑`<br>*لا يوجد مصروف في P&L* | **تحريك مخزني فقط (Stock Movements)**<br>يتم خصم كمية الخام (`Production_Consumption`) وإضافة المنتج الجاهز (`Purchase_Receipt`). | ⚠️ **صحيح مخزنياً، لكنه مشوه مالياً**: حركة الكميات صحيحة، لكن قيم المخزون المالي تتعارض مع قيد المصروف السابق عند الشراء. |
| **3. عملية البيع (Sale Event)** | **حدثان محاسبيان (Two Accounting Events)**<br>A. `Revenue ↑` (سعر البيع)<br>B. `COGS ↑` & `Finished Goods ↓` (تكلفة المنتج) | **تسجيل إيراد + خصم مخزون + حساب تقريبي للـ COGS**<br>- يسجل `Revenue`<br>- يخصم مخزون المنتج الجاهز<br>- يحسب التكلفة بشرط `[COST:]` أو مطابقة الاسم أو بنسبة افتراضية **85%**! | ❌ **خلل**: لا يوجد قيد صريح لـ COGS، واستخدام نسبة 85% افتراضية غير دقيق ويؤدي لاحتساب دبل للمصروف. |
| **4. قائمة الأرباح والخسائر (Profit & Loss / KPIs)** | `Gross Profit = Sales Revenue - COGS`<br>`Net Profit = Gross Profit - Operating Expenses` (إيجارات، أجور، إلخ) | `Total Revenue = SUM(Revenue)`<br>`Total Expense = SUM(Expense)`<br>`Net Profit = Total Revenue - Total Expense` | ❌ **خلل**: يخلط بين تكلفة المشتريات والمصروفات التشغيلية، ولا يعرض مجمل الربح (Gross Profit). |

---

## 3. التحليل التفصيلي لكل مرحلة | Detailed Step-by-Step Analysis

---

### Step 1 — شراء المواد الخام (Buying Material)

#### النموذج المعياري المطلوبة:
- **القيد المحاسبي**:
  - المخزون (مواد خام) `+10,000` (أصل / Asset)
  - النقدية / الموردين `-10,000` (أصل/التزام)
- **الأثر على P&L**: **صفر مصروفات، صفر إيرادات، صفر أرباح**.
- **السبب**: خرجت النقدية لكن حل محلها أصل بنفس القيمة (خشب/مواد).

#### كود النظام الحالي (`PurchaseOrderController.php` & `SupplierController.php`):
- عند إنشاء طلب شراء أو استلامه وتأكيد دفع عربون/مبلغ:
  ```php
  // PurchaseOrderController.php (Lines 120-129 & 198-207)
  Expense::create([
      'expense_number' => $expNo,
      'amount' => $userDeposit,
      'expense_date' => $validated['order_date'] ?? Carbon::now(),
      'category' => 'شراء مواد خام',
      'description' => 'تكلفة دفعة مقدمة لفاتورة مشتريات...',
      'supplier_id' => $validated['supplier_id'],
  ]);
  ```
- **المشكلة الحالية**:
  يتم خصم مبلغ الشراء فوراً من أرباح الشركة كـ `Expense`. إذا اشترت الشركة مواد بـ 100,000 ريال ولم تبيع شيئاً في هذا الشهر، ستظهر نتائج الشهر بخسارة 100,000 ريال رغم أن الشركة تملك خشب بمبلغ 100,000 ريال بالمخزن!

---

### Step 2 — تصنيع المنتجات (Manufacturing & Production)

#### النموذج المعياري المطلوب:
- **القيد المحاسبي**:
  - مخزون مواد خام `-6,000` (أصل)
  - مخزون منتجات تامّة الصنع `+6,000` (أصل)
- **الأثر على P&L**: **صفر مصروفات**.
- **السبب**: لم يتم البيع بعد، تم تغيير شكل الأصل فقط من خشب خام إلى طاولات جاهزة.

#### كود النظام الحالي (`OperationController.php`):
- عند بدؤ الإنتاج (`startProduction`):
  - ينشئ حركة `Production_Consumption` ويخصم الكمية من `materials`.
- عند إتمام الإنتاج (`completeProduction`):
  - ينشئ حركة `Purchase_Receipt` ويضيف الكمية إلى `products`.
- **المشكلة الحالية**:
  على مستوى حركة الكميات المادية (Quantities)، النظام يعمل بشكل جيد جداً. لكن على المستوى المالي، بما أن تكلفة الخام تم تسجيلها بالفعل كـ `Expense` عند الشراء في Step 1، فإن تقييم المخزون المالي (`inventoryValue`) في اللوحة الرئيسية يعيد إضافة قيمة المنتجات والجامدات (`Product::stock_quantity * unit_cost`) مما يسبب تناقضاً بين الميزانية الحسابية وجدول المصروفات.

---

### Step 3 — بيع المنتجات وتكلفة المبيعات (Sell Products & COGS)

#### النموذج المعياري المطلوب:
- عند بيع 10 طاولات بـ 15,000 ريال (وتكلفتها 6,000 ريال):
  - **أ. الإيراد**: النقدية / العملاء `+15,000` | إيراد المبيعات `+15,000`
  - **ب. تكلفة البضاعة المباعة**: تكلفة المبيعات (COGS) `+6,000` | مخزون المنتجات الجاهزة `-6,000`
- **نتيجة الـ P&L**:
  - الإيراد = 15,000
  - تكلفة المبيعات (COGS) = 6,000
  - **مجمل الربح (Gross Profit)** = 9,000

#### كود النظام الحالي (`SalesController.php`):
- عند البيع (`SalesController::store`):
  - يسجل فاتورة إيراد في جدول `revenues`.
  - يخصم الكمية من مخزون `products`.
- عند عرض قائمة المبيعات والربحية (`SalesController::index`):
  - يحاول استنتاج تكلفة المنتج من الوصف عبر نص `[COST: 100]`، أو البحث عن اسم المنتج ومضاعفة `unit_cost`.
  - إذا لم يجد، يستخدم **نسبة افتراضية 85% من سعر البيع** كـ COGS:
  ```php
  // SalesController.php (Lines 94-96)
  if ($productCost == 0) {
      $productCost = (float)$s->amount * 0.85;
  }
  ```
- **المشكلة الحالية**:
  1. الاعتماد على الاستنتاج النصي والنسبة الافتراضية 85% يعطي نتائج غير دقيقة نهائياً للأرباح.
  2. إذا احتسبت التكلفة في المبيعات، ومشتريات الخام مسجلة كـ Expense في Step 1، تم احتساب التكلفة مرتين (Double Counting).

---

### Step 4 — اللوحة الرئيسية والتقرير المالي (Profit & Loss / Dashboard)

#### النموذج المعياري المطلوب:
- **مجمل الربح (Gross Profit)** = الإيرادات (Sales Revenue) - تكلفة البضاعة المباعة (COGS).
- **صافي الربح (Net Profit)** = مجمل الربح - المصروفات التشغيلية (إيجارات، أجور، كهرباء، تسويق).

#### كود النظام الحالي (`DashboardController.php`):
```php
// DashboardController.php (Lines 24-25)
$totalRevenue = (float) Revenue::where('revenue_date', '<=', $targetDate)->sum('amount');
$totalExpense = (float) Expense::where('expense_date', '<=', $targetDate)->sum('amount');
```
- **المشكلة الحالية**:
  - `totalExpense` يجمع كل ما في جدول `expenses` بدون تمييز، فيشمل: (شراء المواد الخام + الإيجارات + الأجور + الفواتير).
  - لا توجد خانة أو مؤشر مستقل لـ **تكلفة البضاعة المباعة (COGS)** ولا لـ **مجمل الربح (Gross Profit)**.

---

## 4. ملخص الأخطاء المحاسبية في النظام الحالي | Summary of Accounting Flaws

1. **الخلط بين التدفق النقدي والمصروف (Cash Flow vs Expense Confusion)**:
   - شراء المواد الخام هو خروج نقدية (Cash Outflow) وليس مصروفاً تشغيلياً (Expense).
2. **احتساب التكلفة مرتين (Risk of Double Counting)**:
   - تسديد قيمة المواد الخام يُسجل كمصروف عند الشراء، ثم تُحسب تكلفة المنتجات المباعة مرة أخرى عند البيع.
3. **غياب مفهوم مجمل الربح (Gross Profit & COGS Missing)**:
   - التقرير المالي الحالي لا يفرق بين التكلفة المباشرة للمنتج (Direct Cost/COGS) والمصروفات العمومية والتشغيلية (Overhead Expenses).
4. **عدم دقة حساب تكلفة المبيعات (Heuristic / Estimated COGS)**:
   - استخدام نسبة 85% افتراضية في `SalesController` يمنع معرفة الربحية الحقيقية للمنتجات.

---

## 5. خريطة الطريق والتوصيات للتعديل | Actionable Refactoring Roadmap

لإصلاح النظام وتطبيقه وفقاً للنموذج المحاسبي الصحيح:

### 1️⃣ تصحيح الشراء في `PurchaseOrderController.php`
- **حذف** إنشاء سجل في جدول `expenses` تحت تصنيف `"شراء مواد خام"`.
- مشتريات المواد الخام تزيد من حساب أصل المخزون (`Material.stock_quantity * unit_cost`) وتخفض النقدية / تزيد دائنية المورد (`Supplier.debt_amount`).

### 2️⃣ تخصيص جدول `expenses` للمصروفات التشغيلية فقط
- يجب أن يحتوي جدول `expenses` على المصروفات الدورية والتشغيلية فقط مثل:
  - الإيجار (Rent)
  - الرواتب والأجور (Salaries & Wages)
  - الكهرباء والمرافق (Utilities & Electricity)
  - التسويق والصيانة (Marketing & Maintenance)

### 3️⃣ تسجيل تكلفة البضاعة المباعة (COGS) صراحةً في `SalesController.php`
- عند إنشاء عملية بيع، يتم حساب COGS بدقة: `COGS = Quantity Sold × Product.unit_cost`.
- تخزين قيمة `cogs` في سجل المبيعات/الإيراد أو في جدول قيود محاسبية بدلاً من الاستنتاج النصي أو النسبة الافتراضية (85%).

### 4️⃣ تحديث اللوحة الرئيسية `DashboardController.php`
- إعادة هيكلة المؤشرات المالية (KPIs):
  - **إجمالي الإيرادات (Total Revenue)** = مجموع المبيعات.
  - **تكلفة المبيعات (Total COGS)** = مجموع تكلفة المنتجات المباعة بالفعل.
  - **مجمل الربح (Gross Profit)** = الإيرادات - COGS.
  - **المصروفات التشغيلية (Operating Expenses)** = مجموع المصروفات التشغيلية.
  - **صافي الربح (Net Profit)** = مجمل الربح - المصروفات التشغيلية.
