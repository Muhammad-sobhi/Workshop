# 🏢 نظام إدارة الموظفين والرواتب المتكامل (Employee & Payroll Management System)

وثيقة شاملة توضح البنية المعمارية، تدفق البيانات، الـ APIs، الجداول وقواعد البيانات، ومكونات الواجهة الأمامية لنظام الموظفين والرواتب وسجل الإنتاج وكشف الحساب.

---

## 📌 الفهرس
1. [نظرة عامة على النظام](#1-نظرة-عامة-على-النظام)
2. [دورة تدفق البيانات والعمليات (Data Flow Architecture)](#2-دورة-تدفق-البيانات-والعمليات-data-flow-architecture)
3. [قاعدة البيانات والمخطط الهيكلي (Database Schema & Models)](#3-قاعدة-البيانات-والمخطط-الهيكلي-database-schema--models)
4. [خدمة الأستاذ العام للموظفين (EmployeeLedgerService)](#4-خدمة-الأستاذ-العام-للموظفين-employeeledgerservice)
5. [واجهات برمجة التطبيقات (Backend RESTful APIs)](#5-واجهات-برمجة-التطبيقات-backend-restful-apis)
6. [الواجهة الأمامية وتصميم النظام (Frontend & Design System)](#6-الواجهة-الأمامية-وتصميم-النظام-frontend--design-system)
7. [دليل الاستخدام والتشغيل (User Workflows)](#7-دليل-الاستخدام-والتشغيل-user-workflows)

---

## 1. نظرة عامة على النظام

يهدف النظام إلى أتمتة كافة العمليات الإدارية والمالية المتعلقة بالعمال والموظفين داخل الورش والمصانع:
- **دعم كافة أنظمة العمل**: عمل باليومية (Daily)، بالقطعة (Piece-rate)، بالنظام المزدوج (Hybrid)، والشهري (Monthly).
- **جدول اليوميات الأسبوعي (Weekly Timesheet Grid)**: تسجيل دوام الموظفين على مدار أيام الأسبوع (من السبت إلى الخميس).
- **سجل الإنتاج بالقطعة (Production Logging)**: تسجيل إنتاج العمال للمنتجات مع احتساب تكلفة العمالة التلقائية وربطها بالأستاذ العام.
- **سجل الأستاذ العام للموظف (Employee Ledger)**: تسجيل كافة القيود المالية (مستحقات دائنة، وسلف/مدفوعات مدينة) مع حساب الرصيد التراكمي الحي لمنع أي تضارب مالي.
- **صرف الرواتب والسلف**: صرف مستحقات الفترات وتسجيل السلف النقدية مع إرفاق الإيصالات وسندات الصرف.

---

## 2. دورة تدفق البيانات والعمليات (Data Flow Architecture)

```mermaid
flowchart TD
    A[بيانات الموظف الأساسية] --> B[يوميات العمل الأسبوعية]
    A --> C[تسجيل الإنتاج بالقطعة]
    A --> D[صرف السلف النقدية]
    
    B -- "حساب اليوميات + القطعة" --> E[ترحيل مستحقات (Credit)]
    C -- "حساب كمية * سعر القطعة" --> E
    D -- "تسجيل سلفة" --> F[ترحيل خصم/سلفة (Debit)]
    
    E --> G[(دفتر الأستاذ العام للموظف\nEmployee Ledger)]
    F --> G
    
    G --> H[حساب الرصيد التراكمي الصافي / الدين المستحق]
    H --> I[شاشة كشف الحساب]
    H --> J[شاشة صرف الراتب]
    
    J -- "صرف الراتب وتسوية الفترة" --> K[ترحيل دفعة الراتب (Debit)]
    K --> G
```

---

## 3. قاعدة البيانات والمخطط الهيكلي (Database Schema & Models)

### أ. جدول الموظفين `employees`
- `id`: المعرف الأساسي.
- `name`: اسم الموظف.
- `phone`: رقم الهاتف.
- `job_title`: المسمى الوظيفي.
- `salary_cycle`: نوع الراتب (`monthly`, `weekly`, `daily`, `piece_rate`).
- `rate`: القيمة الأساسية لليومية أو الراتب.
- `is_active`: حالة الموظف.

### ب. جدول يوميات العمل `employee_attendances`
يسجل تفاصيل الحضور والغياب واليوميات والإنتاج المرتبط باليوم:
- `employee_id`: الموظف.
- `date`: تاريخ اليومية.
- `work_mode`: طبيعة الدوام (`full_day`, `half_day`, `piece_rate`, `hybrid`, `leave`, `absent`).
- `daily_wage`: اليومية المحتسبة.
- `product_id`: المنتج المصنوع (في حالة القطعة/المزدوج).
- `quantity`: الكمية المصنوعة.
- `piece_rate`: سعر القطعة.
- `advance_amount`: سلفة اليوم (إن وجدت).
- `penalty_amount`: الخصم أو الجزاء.
- `task_description`: بيان الأعمال والمهام.

### ج. جدول سجل الإنتاج `employee_production_logs`
- `employee_id`: الموظف المنفذ.
- `product_id`: المنتج المراد تسجيل إنتاجه.
- `date`: تاريخ الإنتاج.
- `quantity_produced`: الكمية المنتجة.
- `piece_rate`: سعر مصنعية القطعة.
- `total_amount`: إجمالي المبلغ المستحق (`quantity * piece_rate`).
- `notes`: ملاحظات الوردية أو أمر التشغيل.

### د. جدول الأستاذ العام للموظفين `employee_ledger_entries`
- `employee_id`: الموظف.
- `entry_date`: تاريخ المعاملة.
- `type`: نوع الحركة (`credit`: مستحق للموظف دائن | `debit`: منصرف للموظف مدين).
- `amount`: قيمة الحركة.
- `running_balance`: الرصيد التراكمي بعد الحركة.
- `source_type`: مصدر القيد (`timesheet`, `production_log`, `salary_payout`, `advance`, `manual`).
- `source_id`: معرف السجل المصدري.
- `description`: البيان التوضيحي للحركة.

### هـ. جدول الرواتب والسلف `employee_salaries`
- `employee_id`: الموظف.
- `type`: نوع المعاملة (`salary` أو `advance`).
- `payment_date`: تاريخ الصرف.
- `base_salary`: الراتب الأساسي أو قيمة السلفة.
- `bonus`: المكافآت.
- `deductions`: الخصومات.
- `net_salary`: المبلغ الصافي المنصرف.
- `payment_method`: طريقة الدفع (`cash`, `bank_transfer`, `check`).
- `receipt_path`: مسار صورة الإيصال المرفق.

---

## 4. خدمة الأستاذ العام للموظفين (EmployeeLedgerService)

الملف: `erp-backend/app/Services/EmployeeLedgerService.php`

توفر هذه الخدمة دوال آمنة ومركزية معالجة في معاملات قاعدة البيانات (`DB::transaction`):

1. **`recordCredit($employeeId, $amount, $date, $sourceType, $sourceId, $description)`**:
   - تسجل استحقاق مالي للموظف (يزيد رصيد الموظف الدائن).
2. **`recordDebit($employeeId, $amount, $date, $sourceType, $sourceId, $description)`**:
   - تسجل مبلغاً منصرفاً للموظف (سلفة أو دفعة راتب تقلل رصيد الموظف).
3. **`getStatement($employeeId, $startDate, $endDate, $perPage)`**:
   - تجلب كشف الحساب المفصل للموظف مع ترقيم الصفحات وحساب الرصيد.
4. **`getCurrentBalance($employeeId)`**:
   - تحسب الرصيد المستحق الحي للموظف (إجمالي الدائن - إجمالي المدين).

---

## 5. واجهات برمجة التطبيقات (Backend RESTful APIs)

| المسار (Endpoint) | الطريقة (Method) | الوظيفة | المعاملات الأساسية |
|---|---|---|---|
| `/api/employees` | `GET` | قائمة الموظفين مع الرصيد الحالي | `search`, `page`, `per_page` |
| `/api/employees/stats` | `GET` | إحصائيات الموظفين والديون والرواتب | — |
| `/api/employees/{id}/timesheet` | `GET` | جلب جدول يوميات الأسبوع لموظف | `week_start` (تاريخ السبت) |
| `/api/employees/{id}/timesheet` | `POST` | حفظ أو تحديث يوميات أسبوع كامل | `week_start`, `days` (مصفوفة الأيام) |
| `/api/employees/{id}/timesheet` | `DELETE` | مسح سجلات أسبوع محدد | `week_start` |
| `/api/employees-production-logs` | `GET` | جلب سجلات الإنتاج بالقطعة | `employee_id`, `per_page` |
| `/api/employees/{id}/production-logs` | `POST` | تسجيل دفعة إنتاج وترحيلها | `date`, `items` (المنتجات والكميات) |
| `/api/employees/{id}/ledger` | `GET` | كشف حساب الأستاذ العام للموظف | `start_date`, `end_date`, `page` |
| `/api/employees/{id}/salaries` | `POST` | صرف راتب أو تسجيل سلفة | `type`, `payment_date`, `amount`, `receipt` |

---

## 6. الواجهة الأمامية وتصميم النظام (Frontend & Design System)

تم بناء الواجهة الأمامية بالكامل بواسطة **React 19 + Tailwind CSS v4** وفقاً للهوية البصرية للـ ERP:
- **لوحة الألوان الأساسية**:
  - خلفية الصفحات: `#171126` (Dark Violet)
  - البطاقات والحاويات: `#231B3D`
  - البطاقات الفرعية والمدخلات: `#2F264C`
  - الحدود والفواصل: `#3D3554`
  - اللون الذهبي الرئيسي: `#ECC796`
  - لون النجاح والأرقام الموجبة: `#13DEB9`
  - لون السلف والخصومات: `text-red-400` / `bg-red-500/10`

### المكونات الرئيسية:
1. **`dashboard/src/pages/employees/page.jsx`**:
   - الصفحة الرئيسية التي تجمع التبويبات الـ 4، بطاقات الـ KPIs العلوية، وإدارة الموظفين والرواتب.
2. **`dashboard/src/components/employees/WeeklyTimesheetGrid.jsx`**:
   - جدول تفاعلي أسبوعي يبدأ من السبت، يدعم التبديل بين الأسابيع، الحسابات الفورية للصافي، وتوليد دفعة الراتب مباشرة.
3. **`dashboard/src/components/employees/ProductionLogGrid.jsx`**:
   - واجهة متعددة الصفوف لإدخال كميات الإنتاج بالقطعة وحساب إجمالي المستحقات وعرض جدول العمليات السابقة.
4. **`dashboard/src/components/employees/EmployeeLedgerModal.jsx`**:
   - نافذة كشف الحساب المنبثقة، تحتوي على 3 بطاقات مؤشرات علوية (إجمالي المستحقات، إجمالي المنصرف، الرصيد المتبقي)، وفلترة التواريخ، وجدول الحركات التفصيلي.

---

## 7. دليل الاستخدام والتشغيل (User Workflows)

### أ. تسجيل اليوميات الأسبوعية وصرف الراتب
1. التوجه لتبويب **"يوميات العمل"**.
2. اختيار الموظف المطلوب وفترة الأسبوع.
3. تحديد طبيعة دوام كل يوم (يوم كامل، نصف يوم، بالقطعة، غياب...).
4. الضغط على **"حفظ الأسبوع"**.
5. الضغط على **"صرف الراتب"** ليتم فتح نموذج الصرف وتعبئة الصافي المستحق تلقائياً وترحيل القيد للدفتر العام.

### ب. تسجيل إنتاج العمال بالقطعة
1. التوجه لتبويب **"سجل الإنتاج"**.
2. اختيار الموظف وتاريخ الإنتاج.
3. إضافة صفوف للمنتجات مع الكميات وسعر المصنعية للقطعة.
4. الضغط على **"حفظ وترحيل المستحق"** ليتم إضافة المستحقات لحساب الموظف فوراً.

### ج. الاطلاع على كشف الحساب المالي
1. في تبويب **"إدارة الموظفين"**، النقر على أيقونة **"كشف حساب"** بجوار أي موظف.
2. استعراض الحركات المدينة والدائنة والرصيد التراكمي الحي.
